import { HighlightHoverDirective } from './highlight-hover.directive';

describe('HighlightHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightHoverDirective();
    expect(directive).toBeTruthy();
  });
});
